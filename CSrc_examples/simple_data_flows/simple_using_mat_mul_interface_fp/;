#include <stdio.h>
#include <stdlib.h>
#include "globals.h"
extern hw_ac **myOp;   

int matmul_unfolded_0 (int A, int B) {
  int sum;
  int   i;
  int   j;
  int   k;
  int mulTemp; 
//------calculating element 0 and 0
sum = 0;
mulTemp = myOp[0]->calc(A, -1*B); //AdditionOp
return mulTemp;
}

#include "input_parser.h"
#include <vector>
#include <cstring>
#include <iostream>
using std::vector;
using std::string;
using namespace std;
enum status {SUCCESS, FAILURE}; 
vector<int> inputVar;
/** 
 * @brief this module is responsible for unit testing the inputParser
 * 
 * @return 
 */
int inputParserTest1(){
    string inputFile = "inputFile.txt" ;

    int status = inputParser(inputFile);
    //    int status = inputParser(inputFile, inputVar);
//    //defiing an array of MyOps 
//    for (int i=0; i<inputVar.size(); i++) {
//        cout<< "this is the " << i << "th value of the input " << inputVar[i];
//    }
    return 1;
}    
 



/** 
 * @brief contains all the unit tests
 * 
 * @return 
 */

int notmain() {
//int main() {
    inputParserTest1();
}
